### 追加された機能

設定画面に「Zenz 補正を直接表示（遅い場合は Mozc を使用）」を追加しました。

- デフォルトはOFFです。OFFのままなら、4.70.0からの通常の動作と変わりません。
- ONにすると、Spaceによる通常変換で、Zenzが十分速く答えられた場合にMozcの候補を途中表示せず、Zenzの結果を直接表示します。
- ライブ変換には影響しません。
- Zenzの対象外、時間内に結果が得られない場合、または結果が採用されない場合は、Mozcの結果を表示します。
- 結果を待っている間は、Spaceを押す直前の入力表示を保ち、候補ウィンドウを一時的に隠します。
- 現在の標準設定では、最大228ミリ秒だけZenzの結果を待ちます。遅れて届いた結果は画面に表示せず、その変換の学習にも使用しません。

### 修正内容

- Windows版で、旧バージョンから更新したときに新しい実行ファイルが正しく置き換わらないことがある問題を修正しました。
- 「バージョン情報」には、ユーザー向けのMozkeyのバージョンだけを表示するようにしました。
- 設定画面で「Zenz 補正を直接表示（遅い場合は Mozc を使用）」とZenzの条件設定が重なって表示される問題を修正しました。
- Zenz直接表示で、表示猶予時間を過ぎた後にキューへ入っていた結果を誤って採用する可能性がある問題を修正しました。期限後の結果は画面表示や学習に使用しません。

### ダウンロード

- Windows x64: `mozkey-space_4.80_windows_x64.msi`
- macOS Universal（Apple Silicon / Intel）: `mozkey-space_4.80_macos_universal.pkg`
- 各ファイルのSHA-256チェックサムも同じReleaseに添付します。

ユーザー向けバージョン: `4.80.2`
内部Mozcバージョン: `3.34.6216.100`

### Windowsへのインストール（重要）

Windows版MSIはコード署名されていないため、環境によってはWindows Defender / SmartScreenに警告またはブロックされる場合があります。

発行元を確認できないという警告だけが表示された場合は、「詳細情報」→「実行」または「実行する」を選択してインストールを続行してください。マルウェア警告が表示された場合は実行しないでください。

### macOSへのインストール（重要）

macOS版PKGはDeveloper ID署名・公証を行っていないため、macOSにインストールを止められる場合があります。

その場合は、「システム設定」→「プライバシーとセキュリティ」を開き、ブロックされたインストーラーの「このまま開く」（または「Open Anyway」）を選んで明示的に承認してください。承認後、PKGをもう一度開きます。この承認を行わない限り、macOS版はインストールできません。
